{
  "framework": "vite",
  "installCommand": "npm install --prefix web",
  "buildCommand": "npm run build --prefix web",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/health",
      "destination": "https://REPLACE_WITH_RENDER_URL/health"
    },
    {
      "source": "/api/:path*",
      "destination": "https://REPLACE_WITH_RENDER_URL/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
