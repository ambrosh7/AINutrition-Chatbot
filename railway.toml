[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Do NOT set startCommand with "--port $PORT" here — Railway may not expand it
# and uvicorn receives the literal string "$PORT". Use Dockerfile CMD (start.sh).
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
